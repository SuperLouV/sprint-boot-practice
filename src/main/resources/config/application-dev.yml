#开发环境配置
server:
  port: 8080
#  context-path: /api
logging:
  level:
    root: info
    com.lyl: debug
  file:
    name: logs/debug.log

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/book?serverTimezone=UTC
    username: root
    password: lyl961008
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
